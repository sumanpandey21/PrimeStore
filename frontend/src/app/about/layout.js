

export const metadata = {
  title: "PrimeStore-Best Online Store in Nepal - About",
  description: "Explore products",
};

export default function Layout({ children }) {
  return (
    <>
      <main>{children}</main>
    </>
  )
}
