export const metadata = {
  title: "Product - PrimeStore-Best Online Store in Nepal",
  description: "Explore products",
};

export default function Layout({ children }) {
  return (
    <>
      <main>{children}</main>
    </>
  )
}
