

export const metadata = {
  title: "Search Results - PrimeStore-Best Online Store in Nepal",
  description: "Explore products",
};

export default function Layout({ children }) {
  return (
    <>
      <main>{children}</main>
    </>
  )
}
