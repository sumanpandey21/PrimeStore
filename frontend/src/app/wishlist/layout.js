

export const metadata = {
  title: "PrimeStore Wishlist",
  description: "Wishlist",
};

export default function Layout({ children }) {
  return (
    <>
      <main>{children}</main>
    </>
  )
}
